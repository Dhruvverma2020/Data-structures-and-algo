int getCount(struct Node* head)
    {
      if(head==NULL)
      {
          return 0;
      }
      Node*curr=head;
      int count=0;
      while(curr!=NULL)
      {
          count++;
          curr=curr->next;
      }
      return count;
    }
